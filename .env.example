# URL publique du site (OG, e-mails, JSON-LD). Ex. https://vitrine.arcos.example
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# SMTP (ex. Gmail / Google Workspace — mot de passe d’application)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=infra@fawd.be
SMTP_PASSWORD=
SMTP_FROM=infra@fawd.be

# Boîte qui reçoit les demandes depuis le formulaire
CONTACT_TEAM_TO=infra@fawd.be
